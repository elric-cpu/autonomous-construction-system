name: Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Unzip project archive
        run: unzip -qq autonomous-construction-full.zip -d extracted

      - name: Install Node dependencies
        run: |
          cd extracted/benson-agentic-system
          npm ci
          npm test --if-present || true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          cd extracted/construction_agentic_system
          pip install -r requirements.txt
          pytest || true
